name: dependabot-auto-merge

on: pull_request

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    if: ${{ github.actor == 'dependabot[bot]' }}
    uses: misaf/.github/.github/workflows/dependabot-auto-merge.yml@0d6486e765021789962e1280e9f81f132aaedca8 # main
